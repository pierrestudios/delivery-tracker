import React from "react";
import { mount } from "enzyme";

import ReserveForm from "./ReserveForm";

describe("Renders <ReserveForm /> with params correctly ", () => {
  const params = {};

  it("Renders <ReserveForm /> with correct ", () => {
    const wrapped = mount(<div />);
  });
});
